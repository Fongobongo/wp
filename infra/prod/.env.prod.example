DOMAIN=play.example.com
LE_EMAIL=ops@example.com

# For Caddy basicauth routes (/metrics and /services/*).
BASIC_AUTH_USER=ops
# Generate with: docker run --rm caddy:2.8-alpine caddy hash-password --plaintext 'change-this-password'
BASIC_AUTH_PASSWORD_HASH=<PASTE_BCRYPT_HASH_HERE>
